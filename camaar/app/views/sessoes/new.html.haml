%h1 Login

= form_with url: login_path, method: :post, local: true do |f|
  .field
    = f.label :email
    = f.email_field :email

  .field
    = f.label :senha
    = f.password_field :senha

  .actions
    = f.submit "Entrar"
